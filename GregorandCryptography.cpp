#include <bits/stdc++.h>

#define ll long long
const ll mod = 1000 * 1000 * 1000 + 7;

using namespace std;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);

    long long t, p;
    cin >> t;

    while (t--)
    {
        cin >> p;

        cout << 2 << " " << p - 1 << endl;
    }

    return 0;
}